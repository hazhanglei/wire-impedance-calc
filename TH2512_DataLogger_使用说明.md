# TH2512+ 插拔测试程序 V7 修复说明

## 本次修复内容

### 问题1：首次启动不记录
**原因**：`prev_over` 是局部变量，每次 `_start()` 调用后都是 `None`，导致第一次有效跳变无法检测。

**修复**：
- 将 `prev_over` 改为实例变量 `self.prev_over`
- 在 `_start()` 中立即读取一次确定初始状态

### 问题2：EXE打包占用文件
**原因**：上一次运行的EXE进程未完全退出。

**修复**：已清理，当前可正常覆盖。

---

## 工作原理

```
_start() 调用时:
  1. 立即读取一次仪器响应
  2. 判断当前是"插入"还是"拔出"状态
  3. 设置 self.prev_over = True/False
  4. 启动采集线程

采集线程:
  每次读取 -> 判断跳变 -> 记录结果
  - 超量程->有效值: 标记插入，保存当前值
  - 有效值->超量程: 标记拔出，记录最后有效值
```

---

## 使用方法

1. 运行 `TH2512_DataLogger.exe`
2. 选择COM口 → 点击"连接串口"
3. 点击"▶ 开始测试"（此时会立即读取一次，确定初始状态）
4. 插入探针 → 界面显示"测试中..."
5. 拔出探针 → 自动记录一条结果
6. 重复4-5进行多次测试
7. 点击"■ 停止测试" → 数据自动保存

---

## 文件位置

```
E:\Hermes\00-配置档案总库\默认配置\04-成品导出目录\TH2512_DataLogger.exe
```

---

*版本: V7.0 | 修复日期: 2026-08-14*
