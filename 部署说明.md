# GitHub Pages 部署说明

## 自动部署（推荐）

```bash
cd /e/Hermes/00-配置档案总库/默认配置/04-成品导出目录
./deploy.sh
```

## 手动部署步骤

### 1. 启用GitHub Pages
访问：https://github.com/hazhanglei/wire-impedance-calc/settings/pages

选择分支：`main`，文件夹：`/(root)`，点击Save

### 2. 等待构建
约1-2分钟后访问：
**https://hazhanglei.github.io/wire-impedance-calc/**

---

## 临时方案：本地HTTP服务器

如果网络问题导致GitHub推送失败，可使用本地服务器：

```bash
cd /e/Hermes/00-配置档案总库/默认配置/04-成品导出目录
python serve_wire_calc.py
```

然后在手机浏览器访问：`http://<本机IP>:8765`

查看本机IP：
```bash
ip addr show | grep "inet " | grep -v 127.0.0.1
```

---

## 数据共享方式

由于iOS Safari限制，数据通过剪贴板共享：

**导出：**
1. 点击「📦 导出」
2. 复制生成的JSON文本

**导入：**
1. 点击「📥 导入」
2. 粘贴JSON文本
3. 点击「导入」

---

## iOS使用提示

1. 将HTML文件通过AirDrop发送到iPhone
2. 用Safari打开
3. 添加到主屏幕（分享按钮 → 添加到主屏幕）
4. 从主屏幕打开可获得全屏体验
